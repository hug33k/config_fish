#!/usr/bin/env fish
# -*-  mode:fish; tab-width:4  -*-

set FLINE_PATH	  $HOME/.config/fish/fishline

set -gx SHELL	  /usr/local/bin/fish

set -gx TERM	  "xterm-256color"
set -U EDITOR	  "emacs"
set -gx MAIL	  "/u/all/"$USER"/mail/"$USER
set -U PAGER	  "less"
set -U BROWSER	  "google-chrome-beta"
